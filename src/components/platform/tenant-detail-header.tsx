import Link from "next/link";
/* eslint-disable @next/next/no-img-element -- White-label logos may be stored as data URLs. */
import { Button } from "@/components/ui/button";
import type { TenantStatus } from "@/types/tenant.types";

export const statusLabels: Record<TenantStatus, string> = { TRIAL: "تجريبي", ACTIVE: "نشط", SUSPENDED: "موقوف", ARCHIVED: "منتهي" };
export function TenantDetailHeader({ tenant }: { tenant: { id: string; name: string; slug: string; status: TenantStatus; plan: string; branding: { logo: string } } }) { return <div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div className="flex items-center gap-4"><div className="flex h-16 w-16 items-center justify-center rounded-2xl bg-[#F3F4F6] p-2"><img src={tenant.branding.logo} alt="" className="h-full w-full object-contain" /></div><div><h1 className="text-2xl font-black">{tenant.name}</h1><p className="mt-1 font-mono text-xs text-muted-foreground">{tenant.slug} · {tenant.id}</p><div className="mt-2 flex gap-2"><span className="rounded-full bg-[#111111]/10 px-2.5 py-1 text-xs font-bold">{statusLabels[tenant.status]}</span><span className="rounded-full bg-[#6B7280]/15 px-2.5 py-1 text-xs font-bold text-[#374151]">{tenant.plan}</span></div></div></div><Button asChild variant="outline"><Link href="/platform/tenants">العودة للكافيهات</Link></Button></div>; }
export function TenantTabs({ id, active }: { id: string; active: string }) { const tabs = [["overview", "نظرة عامة", ""], ["branding", "الهوية", "/branding"], ["features", "المميزات", "/features"], ["subscription", "الاشتراك", "/subscription"], ["settings", "الإعدادات", "/edit"]]; return <div className="mt-8 flex gap-2 overflow-x-auto border-b pb-2">{tabs.map(([key, label, suffix]) => <Link key={key} href={`/platform/tenants/${id}${suffix}`} className={`shrink-0 rounded-lg px-4 py-2 text-sm font-bold ${active === key ? "bg-[#111111] text-white" : "text-[#374151] hover:bg-[#F3F4F6]"}`}>{label}</Link>)}</div>; }
