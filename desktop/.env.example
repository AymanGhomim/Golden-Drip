# Development only. Production packaging must receive the real shared Backend URL.
VITE_API_BASE_URL=https://api.example.com/api/v1

# Optional development-adapter password. Never use this as production authentication.
VITE_DESKTOP_DEV_PASSWORD=
